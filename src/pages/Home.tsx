// pages/Home.tsx
import React from "react";
import MainContent from "../components/MainContent";

// Home page - Main entry point of the application
const Home: React.FC = () => {
  // Renders the MainContent component as the page's core content
  return <MainContent />;
};

export default Home;